pluginManagement {
    repositories {
        google()              // ← здесь появилось Google Maven
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'my_native_plugin'

include(":flutter")
project(":flutter").projectDir = new File(settingsDir, "../../example/")

